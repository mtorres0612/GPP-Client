using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace GPPClient.Utilities
{
    public enum TransmissionTypes
    {
        FTP     = 1,
        SFTP    = 2,
        EMAIL   = 3,
        NETWORK = 4,
        HTTP    = 5
    }
}