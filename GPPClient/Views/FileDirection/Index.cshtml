@model IEnumerable<GPPClientModel.FileDirection>

@{
    ViewBag.Title = "File Direction";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FileDirectionCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FileDirectionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FileDirectionCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FileDirectionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.FileDirectionCode }) |
            @Html.ActionLink("Details", "Details", new { id = item.FileDirectionCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FileDirectionCode })
        </td>
    </tr>
}

</table>
